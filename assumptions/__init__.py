# Makes assumptions/ importable so `from assumptions import worldviews` works.
# The numbered assumption files are NOT modules (their names start with digits,
# deliberately — the number is the craziness ordering); worldviews.py execs them.
